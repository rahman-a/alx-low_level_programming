#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @num1: first parameter
 * @num2: second parameter
 *
 * Return: the result of addtion of two arguments
*/
int add(int num1, int num2)
{
	return (num1 + num2);
}
